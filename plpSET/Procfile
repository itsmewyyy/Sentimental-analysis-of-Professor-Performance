web: celery -A backendSet worker --pool=threads --loglevel=info & python manage.py migrate && gunicorn backendSet.wsgi  --bind 0.0.0.0:$PORT
web: celery -A backendSet beat --loglevel=info  --bind 0.0.0.0:$PORT